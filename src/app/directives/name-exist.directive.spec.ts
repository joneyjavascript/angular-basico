import { NameExistDirective } from './name-exist.directive';

describe('NameExistDirective', () => {
  it('should create an instance', () => {
    const directive = new NameExistDirective();
    expect(directive).toBeTruthy();
  });
});
